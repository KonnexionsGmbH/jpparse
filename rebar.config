{cover_enabled, true}.
{cover_excl_mods, [jsonpath_lex, jpparse]}.

{deps_error_on_conflict, true}.

{eunit_opts, [
    {skip_deps, true},
    verbose
]}.

{xrl_opts, [dfa_graph, verbose]}.

{yrl_opts, [verbose]}.
